<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Teal background circle -->
  <circle cx="32" cy="32" r="32" fill="#1a5252"/>
  <!-- White inner circle (the badge) -->
  <circle cx="32" cy="32" r="26" fill="#ffffff"/>
  <!-- FC in teal — matches header badge exactly -->
  <text x="32" y="40" font-family="Georgia, serif" font-size="22" font-weight="bold"
        fill="#1a5252" text-anchor="middle" letter-spacing="-1">FC</text>
</svg>
